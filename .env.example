# Speech-to-text
SARVAM_API_KEY=

# Generation (primary + fallback)
GROQ_API_KEY=
GEMINI_API_KEY=

# Hugging Face Hub token, used to pull prebuilt index artifacts at startup
HF_TOKEN=
# Defaults to gauravxsuvo/groundline-index in config.py, set only to override.
HF_DATASET_REPO=

# Runtime
PORT=8000
