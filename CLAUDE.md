# Claude Code context

See `AGENT.md` for the full project map, pipeline, run instructions, and conventions. Read that first.

Nothing Claude Code specific beyond what's in `AGENT.md` - keep it that way rather than duplicating context across both files.
